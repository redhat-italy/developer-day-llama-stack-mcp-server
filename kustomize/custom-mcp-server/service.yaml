apiVersion: v1
kind: Service
metadata:
  name: custom-mcp-server
  labels:
    app: custom-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: custom-mcp-server